# {{PROJECT_NAME}}

A `dudamel` project — a personal AI assistant that runs on your own
hardware.

## Quickstart

```
uv run dudamel apps list
uv run dudamel run
```

This project starts with no apps: first-party apps that ship with dudamel
are switched on in `dudamel.toml` under `[apps.<name>]` (`apps list` shows
what is available), and your own apps live in `apps/` and are registered in
`assistant.py`.

Once an app of your own defines models, generate a migration for them:

```
uv run dudamel db migrate -m init
```

Until then that command prints `no changes` — there is nothing to
autogenerate. `dudamel run` applies any pending app migrations on startup,
so once a revision exists you don't need to run `db migrate` again unless
you change an app's models.

The dashboard comes up at `http://127.0.0.1:8787` (see `dudamel.toml`).
Log in with the token in `.env` (`DUDAMEL_WEB_TOKEN`) — rotate it any time
with `uv run dudamel token rotate`.

## Diagnosing problems

```
uv run dudamel doctor
```

Checks the database, migration state, each configured LLM tier, Telegram
and web token configuration, and Tailscale reachability, then prints every
registered native tool with its safety flags (`read_only` / `confirm` /
`origin`). MCP-mounted tools aren't in that table — they're only
discoverable by actually starting the assistant (`dudamel run`) — so
`doctor` instead notes how many MCP servers are configured, if any.

## Running as a service

`deploy/dudamel.plist` (launchd, macOS) and `deploy/dudamel.service`
(systemd user unit, Linux) already have this project's path filled in —
see the comments at the top of each for install steps. Both restart
`dudamel run` automatically if it exits. On macOS, `caffeinate` or a Power
Nap / "wake for network access" setting keeps the machine from sleeping out
from under the launchd agent.

## Project layout

- `assistant.py` — registers your apps; `dudamel run`/`db migrate`/`doctor`
  all import it.
- `apps/` — your own apps go here (see `dudamel apps list` for the bundled
  ones).
- `dudamel.toml` — LLM tiers, web/telegram/router configuration, and the
  `[apps.*]` blocks that switch bundled apps on.
- `pyproject.toml` — makes this a `uv`-managed project so `uv run dudamel
  ...` works here.
- `migrations/` — autogenerated app-schema migrations (`dudamel db migrate`).
- `deploy/` — launchd/systemd templates for running as a background service.

See the `dudamel` package documentation for the full guide, including the
security model (confirm gates, the taint rule, budgets) and remote access
over Tailscale.
